define((function() {
    'use strict';

    //var RE_EXTENSION = /\.([0-9a-z]+)(?:[\?#]|$)/i;

    return {
        /*
    	extension: function(filename) {
    		var match = filename.match(RE_EXTENSION);
    		if (match && match[1]) {
    			return match[1];
    		}
    	}
		*/
    };
}()));