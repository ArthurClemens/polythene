{
  "name": "tests-render-react-standalone",
  "description": "",
  "scripts": {
    "dev": "http-server -p 3500",
    "build": "yarn run update",
    "update": "cd ../polythene-react && yarn run build-standalone && cd - && cp ../polythene-react/dist/polythene-react-standalone.js* js/"
  },
  "devDependencies": {
    "http-server": "^0.10.0"
  }
}
