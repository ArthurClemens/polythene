import { ViewComponent } from 'polythene-mithril-base';
import { coreSVG } from 'polythene-core-svg';

const SVG = ViewComponent(Object.assign({}, coreSVG));
SVG.displayName = "SVG";

export { SVG };
