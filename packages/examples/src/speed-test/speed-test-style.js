
const baselineSize = '120px';

const styles = [{
    '.example-speed-test': {
        ' .click-time': {
            margin: '0 0 20px 0'
        },
        ' .baseline': {
            height: baselineSize,
            'line-height': baselineSize,
            width: baselineSize,
            'text-align': 'center',
            background: '#fff'
        }
    }
}];

export default styles;
