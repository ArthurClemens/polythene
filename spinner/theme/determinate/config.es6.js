import common from 'polythene/config/config';

export default {
    size: 40,
    circle_stroke_width: 4,
    animation_update_duration: .5, // seconds

    color_light: common.rgba(common.color_primary),
    color_dark: common.rgba(common.color_primary)
};
